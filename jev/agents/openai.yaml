interface:
  display_name: "Jev Bridge"
  short_description: "Use Jev through your agent"
  default_prompt: "Use $jev to prepare and execute the judgments needed for this task, then explain the actual results."
policy:
  allow_implicit_invocation: true
