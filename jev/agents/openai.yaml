interface:
  display_name: "Jev Bridge"
  short_description: "Use Jev through your agent"
  default_prompt: "Use the official TypeSafe skill for design and $jev for bridge execution, then explain the actual results."
policy:
  allow_implicit_invocation: true
