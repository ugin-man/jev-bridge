# ローカルCodexへの引き継ぎ

利用者の目的は「日本語でやりたいことを伝えるだけで、あなたがJev用に変換・実行・説明する」こと。JSONやコードを利用者に書かせない。主モデルへの完全置換やResponses中継サーバーの改造ではない。

1. このフォルダーのREADME_JA.mdとjev/SKILL.mdを読む。
2. Python 3.11以上の既存実体を確認する。新しいランタイムを勝手にダウンロードしない。
3. `install_skill.py`を実行する。標準の対象はユーザーの `.agents/skills/jev`。スキルフォルダー以外の設定は変更しない。同名の異なるスキルがあれば差分を確認し、許可なく上書きしない。
4. testsを実行する。これは模擬応答によるオフライン検査。実APIの接続成功と呼ばない。
5. 実際のCodexで「Jevスキルを使って」がスキルを読み込むか確かめる。ファイルの配置成功とアプリ側の認識は分ける。必要なら再起動を案内する。
6. インストール先 `jev/scripts/jev.py status` を実行し、キーの値を出さずに準備状態と保存先を確認する。スクリプトが既存Worker/Helperのキーと設定を利用しても、そのコード・モデル・プロファイル・MCPを改変しない。
7. 同梱assets/smoke.jsonをvalidateする。`run`に--executeを付けなければ外部通信はしない。
8. 実接続をユーザーが依頼し、サンプル内容とTypeSafeの別途API使用量を了承した場合だけ、同じサンプルを1回実行する。結果ファイルの重複やタイムアウトでは再送せず、状況を確認する。セットアップ依頼だけでこの実送信を行わない。
9. 実行結果を日本語で説明する。型、フィールド、JSONをユーザーに直させない。材料・判断基準そのものの不足だけ、自然な日本語で聞く。

キー未設定なら、利用者にローカルのSET-KEY.cmdを開いてもらう。キーをチャットに貼るよう求めず、秘密の読み上げ・ログ出力・他の場所への複製をしない。Windows以外のDPAPIを無理に復号せず、実行環境の違いを説明する。

このスキルの自然文処理はホストLLMによる。スクリプトだけに自然文を渡して解釈させる設計ではない。追加のOpenAI APIキーは不要。

報告は「スキル配置」「スクリプト検査」「アプリでのスキル認識」「本物のJev応答」を分ける。実行していない項目を済んだことにしない。
